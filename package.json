{
  "name": "fisica-campus-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "prisma generate --schema prisma/schema.prisma",
    "dev": "nest start --watch",
    "build": "tsc -p tsconfig.build.json && tsc -p tsconfig.seed.json",
    "start": "node dist/main.js",
    "start:prod": "node dist/main.js",
    "start:railway": "node dist/main.js",
    "docker:build": "docker build -t fisica-campus-backend .",
    "prisma:generate": "prisma generate --schema prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema prisma/schema.prisma",
    "prisma:deploy": "prisma migrate deploy --schema prisma/schema.prisma",
    "prisma:seed": "node dist/prisma/seed.js",
    "db:setup": "npm run prisma:deploy && npm run prisma:seed",
    "railway:predeploy": "npm run db:setup"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.4",
    "@nestjs/config": "^4.0.2",
    "@nestjs/core": "^11.1.4",
    "@nestjs/mapped-types": "^2.1.0",
    "@nestjs/platform-express": "^11.1.4",
    "@prisma/client": "6.19.3",
    "@supabase/supabase-js": "^2.105.3",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "prisma": "6.19.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.10",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
